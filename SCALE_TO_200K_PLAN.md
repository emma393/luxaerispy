# Scaling LuxAeris toward 200K+ SEO pages

Current package:
- fixes the logo/header mismatch on generated pages
- keeps the final working static website
- adds a larger real-data starter set

To reach 200K+ pages without publishing weak or fake content, expand in this order:
1. airports
2. routes
3. lounges
4. airline cabin pages
5. aircraft pages
6. flight-number pages
7. city and destination subpages

The repo is structured so these can be added by expanding the JSON data files under /data and rebuilding.
