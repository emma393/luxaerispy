This pack continues the workable core-site fix by adding more destination pages, airline pages, airport pages, and premium route pages.
It also expands the internal site search index so the new pages are discoverable from search.html.
Merge into your LuxAeris root and replace existing versions of these files.
