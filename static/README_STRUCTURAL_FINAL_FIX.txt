This focused fix pack restores the shield logo and favicon assets, adds a homepage search with one-way / round-trip / multi-city selection, fixes request-page prefilling, replaces meta-explanation text with actual page information on the included pages, and makes the visible tools actually function with client-side data.
Merge into your LuxAeris root and replace existing versions of these files.
