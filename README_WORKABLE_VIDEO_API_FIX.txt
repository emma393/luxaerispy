This pack adds the uploaded MP4 as the homepage background video with a faded overlay, restores the shield logo and favicon, keeps request navigation visible, fixes homepage search to prefill the request page, adds one-way / round-trip / multi-city switching, and upgrades the tool pages so they return usable output. It also adds a Netlify function at netlify/functions/flight-status.js for live AviationStack lookup when AVIATIONSTACK_API_KEY is configured.
